# Content score (percent of content pixels matching Chrome) per page, to two
# decimals. The test fails if a score differs in either direction: below is a
# regression; above means the expectation must be raised in the same commit.
# Lowering one needs a comment on its line, checked by CI:
#   name score  # lowered: reason
# Rules: the project rules file, section "Referans testi".
blocks 100.00
merhaba 91.26
paragraphs 48.27  # lowered: metric tightened (tolerance 24 to 12), not a rendering change
